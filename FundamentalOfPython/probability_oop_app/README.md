# Probability OOP Example

Install dependencies:

```bash
python -m pip install -r requirements.txt
```

Run with one configuration format:

```bash
python app.py env
python app.py json
python app.py yaml
```

If no type is supplied, JSON is used:

```bash
python app.py
```

The `logs/` folder and `logs/distributions.log` file are created automatically.
